version: "3.8"

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./sample_data:/app/sample_data
      - ./config:/app/config
    restart: unless-stopped
